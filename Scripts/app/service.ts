module EM.Service {
        export var main = {
            getRootMembers: rootPath + "main/getrootmembers/",
            getChildren: rootPath + "main/getchildren/"
        }
        export var timeRegistration = {
            getRootMembersWithTimeRegistrations: rootPath + "timeregistration/getrootmemberswithtimeregistrations/"
        }
    export var rootPath = "api/timetracking/";
}
